export default {
  title: 'A fila não abriu',
  body: 'Algo quebrou ao montar esta tela. Se acontecer de novo, avise o time com a mensagem abaixo.',
  retry: 'Limpar preferências e tentar de novo',
  /** Names what the button throws away, so the click is not a leap of faith. */
  retryHint: 'Descarta colunas, agrupamento e menu recolhido — não toca nos chamados.',
  /** Only on the layout fallback: there the sidebar is gone, so this is the
   *  only way out. */
  exit: 'Sair do Pipodesk',
}
